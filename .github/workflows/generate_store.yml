name: Generate Store Catalog

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-catalog:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Run Catalog Script
        run: python generate_store.py

      - name: Commit and Push Spawned Folders
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # Stage the new dist folder and its contents
          git add dist/
          
          # Only commit if there are actual structural changes
          if ! git diff --cached --quiet; then
            git commit -m "Automated update: Spawned catalog distribution folders"
            git push
          else
            echo "No changes detected in app catalog."
          fi
